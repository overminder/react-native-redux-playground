{
  "name": "app",
  "version": "0.1.0",
  "description": "1. `npm install && react-native start` 2. `cd ios && pod update && open Example.xcworkspace` for iOS, or    `cd android && ./gradlew installDebug` for Android",
  "main": "index.android.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "repository": "https://github.com/overminder/react-native-redux-playground",
  "dependencies": {
    "core-decorators": "0.12.2",
    "react": "~15.3.0",
    "react-native": "~0.32.0-rc.0",
    "react-redux": "^4.4.5",
    "redux": "^3.5.2",
    "redux-thunk": "^2.1.0",
    "react-native-webview-bridge": "git+https://github.com/overminder/react-native-webview-bridge.git"
  },
  "devDependencies": {
    "tslint": "^3.13.0",
    "typescript": "2.1.0-dev.20160729"
  }
}
